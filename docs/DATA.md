# Data provenance map

Where every number on the dashboard comes from, and which code touches it.
Read this before reviewing the code.

## The flow

```
docs/research/*  (frozen research inputs, never read by the site)
      │
      ├─ etl/seed_from_research.py   (manual, run on research re-issue)
      │        ↓ writes
      │   data/comps.json            (hand-curated seed + analysis layer)
      │   data/ritter_cohorts.json   (converted from ritter_cohorts.csv)
      │
      ├─ etl/refresh_prices.py       (daily) → data/prices.json
      │        also fills ETL-writable fields in comps.json (see rules)
      │
      └─ etl/edgar_watch.py          (daily) → data/filing_status.json

site/index.html  reads ONLY data/*.json. No analysis logic lives in the page
beyond formatting and one derived chart coloring.
```

## Files

| File | Producer | Contents | Provenance |
|---|---|---|---|
| `docs/research/03-research-findings.md` | LLM research run (v2, 2026-06-10) | 727-line findings: SpaceX deal card, Anthropic/OpenAI S-1 watch, valuation anchors, Ritter stats, conflicts | Every claim carries a URL; ◆ = computed, ~ = estimate |
| `docs/research/comps.csv` | Research WP-4 | 17 comps: offer price, day-1 close, +365d price, returns vs offer | EDGAR 424B4 primary + finance data |
| `docs/research/ritter_cohorts.csv` | Research WP-1 | 46 rows, 1980–2025 IPO cohort stats | Jay Ritter (U. Florida) PDFs, May 2026 |
| `docs/01..04-*.md` | Original Cowork session | Spec, research plan, v1 findings, handoff | Superseded by v2 where they differ |
| `data/comps.json` | `seed_from_research.py` | deals, mega_ipos, tech_comps, cohorts, **analysis** (red flags / counterpoints / timeline / base rates) | Each record: `source_url`, `conflict_note`, `as_of` |
| `data/prices.json` | `refresh_prices.py` | Daily closes, computed day-1/+365d/to-date per ticker; full SPCX series | yfinance (Yahoo), unofficial |
| `data/filing_status.json` | `edgar_watch.py` | Public S-1/424B4 status for Anthropic, OpenAI, SpaceX (CIK 1181412) | SEC EDGAR FTS + submissions API |
| `data/ritter_cohorts.json` | `seed_from_research.py` (conversion step in repo history) | Same as CSV, JSON-ified | Ritter |

## Integrity rules (enforced in code — review these paths)

1. **ETL never edits historical seed fields.** `refresh_prices.py` writes only
   `_meta.etl_writable_fields` (`one_yr_return_pct`, `return_to_date_pct`,
   `verified`, `last_price`, `last_price_as_of`, and `day1_return_pct` only
   when the seed is null).
2. **Return basis.** Tech comps: vs split-adjusted **offer price** (WP-4
   convention; yfinance is split-adjusted, so bases match). Mega IPOs: vs
   **first close** (no offer prices in seed) — labeled in the UI.
3. **Divergence tripwire.** If a computed figure differs from a `verified`,
   non-quarantined seed by >20%, the run aborts WITHOUT writing `comps.json`
   and exits non-zero — nothing persists until the discrepancy is resolved.
   Five WP-4 `+365d` figures carry `one_yr_quarantined: true` (BABA, V,
   SNAP, COIN, CRWV): their seed value is kept, the daily ETL skips the
   overwrite and does not re-trip on them, and each `conflict_note` shows
   both values. V and COIN look like WP-4 date errors; flagged for
   re-research.
4. **Conflicts are shown, never averaged.** `conflict_note` → ⚠ tooltip.
5. **No fabrication.** Anything not in the research inputs renders
   "n/a — see findings doc".
6. **The analysis layer is data, not code.** Red flags / counterpoints /
   timeline / base rates live in `comps.json → analysis`, each item with a
   `source_url`, generated by `seed_from_research.py`. The page just renders
   them. To audit a claim, follow its `[src]` link. Items with `derived:
   true` are arithmetic on sourced figures (e.g. unlock dates = Jun 12 + 70/90/… days).

## Known data caveats

- Anthropic/OpenAI financials are all secondary-source until their S-1s go
  public (no audited filings exist). ARR ≠ GAAP revenue.
- Cerebras offer price ~$155 is the range midpoint; exact pending 424B4.
- Ritter "% below offer" (56.1% / 44.5%) is the 1975–2021 **aggregate**;
  year-specific 1999–2000 needs IPOALL.xlsx (listed in findings §6.7).
- SPCX has no price data until after 2026-06-12; the ETL hard-stops with a
  report if data is still missing after that date.
